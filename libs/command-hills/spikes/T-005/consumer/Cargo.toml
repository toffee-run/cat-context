[package]
name = "spike-consumer"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", default-features = false, features = ["derive", "std"] }
spike-lib = { path = "../lib-crate" }
