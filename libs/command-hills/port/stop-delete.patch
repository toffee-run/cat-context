diff --git a/Cargo.toml b/Cargo.toml
index 9979052..3c6094d 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -8,6 +8,7 @@ version = "0.1.0"
 edition = "2024"
 
 [dependencies]
+command-hills = { path = "libs/command-hills" }
 bollard = { version = "0.21.1", default-features = false, features = ["http", "pipe", "ssh", "ssl"] }
 clap = { version = "4.6.6", default-features = false, features = ["unstable-ext", "error-context", "std", "suggestions", "usage", "derive", "env", "string"] }
 clap_complete = { version = "4.6.9", default-features = false, features = ["unstable-dynamic"] }
diff --git a/src/cli.rs b/src/cli.rs
index f2a8899..c1aaeb2 100644
--- a/src/cli.rs
+++ b/src/cli.rs
@@ -7,6 +7,7 @@ use clap_complete::engine::{ArgValueCandidates, ArgValueCompleter, PathCompleter
 
 use crate::ask;
 use crate::{Action, Agent, Base, Command, Prompt, complete};
+use command_hills::ResolveWithCtx;
 
 #[derive(Parser, Debug)]
 #[command(name = "cat-context", version)]
@@ -32,9 +33,9 @@ enum ActionArgs {
     #[command(about = "пересоздать контейнер")]
     Restart(RestartArgs),
     #[command(about = "остановить контейнер")]
-    Stop(TargetArgs),
+    Stop(StopArgs),
     #[command(about = "удалить контейнер")]
-    Delete(TargetArgs),
+    Delete(DeleteArgs),
 }
 
 #[derive(Args, Debug, Default)]
@@ -77,6 +78,46 @@ struct TargetArgs {
     container: Option<String>,
 }
 
+#[derive(Args, Debug, Default)]
+struct StopTargetArgs {
+    #[command(flatten)]
+    target: TargetArgs,
+}
+
+impl ResolveWithCtx<Docker, String> for StopTargetArgs {
+    async fn resolve(self, docker: &Docker) -> command_hills::Result<String> {
+        ask::container(docker, self.target.container, "Какой контейнер остановить?").await
+    }
+}
+
+#[derive(Args, Debug, Default)]
+struct DeleteTargetArgs {
+    #[command(flatten)]
+    target: TargetArgs,
+}
+
+impl ResolveWithCtx<Docker, String> for DeleteTargetArgs {
+    async fn resolve(self, docker: &Docker) -> command_hills::Result<String> {
+        ask::container(docker, self.target.container, "Какой контейнер удалить?").await
+    }
+}
+
+#[derive(Debug)]
+#[command_hills::fill(variant = Action::Stop, context = Docker)]
+struct StopArgs {
+    #[command(flatten)]
+    #[hill(ctx)]
+    container: StopTargetArgs,
+}
+
+#[derive(Debug)]
+#[command_hills::fill(variant = Action::Delete, context = Docker)]
+struct DeleteArgs {
+    #[command(flatten)]
+    #[hill(ctx)]
+    container: DeleteTargetArgs,
+}
+
 #[derive(Args, Debug, Default)]
 #[group(multiple = false)]
 struct PromptArgs {
@@ -175,8 +216,8 @@ mod argument_tests {
             panic!("ожидался delete");
         };
 
-        assert_eq!(stop.container.as_deref(), Some("one"));
-        assert_eq!(delete.container.as_deref(), Some("two"));
+        assert_eq!(stop.container.target.container.as_deref(), Some("one"));
+        assert_eq!(delete.container.target.container.as_deref(), Some("two"));
     }
 
     #[test]
@@ -317,8 +358,8 @@ async fn fill(args: ActionArgs, docker: &Docker) -> ask::Result<Action> {
     match args {
         ActionArgs::Start(args) => start(args),
         ActionArgs::Restart(args) => restart(args, docker).await,
-        ActionArgs::Stop(args) => stop(args, docker).await,
-        ActionArgs::Delete(args) => delete(args, docker).await,
+        ActionArgs::Stop(args) => args.resolve(docker).await,
+        ActionArgs::Delete(args) => args.resolve(docker).await,
     }
 }
 
@@ -455,15 +496,6 @@ mod fill_tests {
     }
 }
 
-async fn stop(args: TargetArgs, docker: &Docker) -> ask::Result<Action> {
-    let container = ask::container(docker, args.container, "Какой контейнер остановить?").await?;
-    Ok(Action::Stop { container })
-}
-
-async fn delete(args: TargetArgs, docker: &Docker) -> ask::Result<Action> {
-    let container = ask::container(docker, args.container, "Какой контейнер удалить?").await?;
-    Ok(Action::Delete { container })
-}
 
 fn ask_action() -> ask::Result<ActionArgs> {
     let chosen = ask::action(action_choices())?;
